# 배포 자동화 설정

이 저장소는 `main` 브랜치에 push되면 GitHub Actions가 Vercel production 배포를 실행하도록 구성한다.

## 동작 방식

1. 로컬에서 원고 또는 설정을 수정한다.
2. 변경사항을 commit하고 `origin/main`으로 push한다.
3. `.github/workflows/vercel-production-deploy.yml` 워크플로우가 실행된다.
4. Vercel CLI가 production 환경을 가져오고, 빌드한 뒤 production으로 배포한다.

## GitHub Secrets

GitHub 저장소의 `Settings > Secrets and variables > Actions > Repository secrets`에 아래 값을 등록해야 한다.

| Secret | 설명 |
|---|---|
| `VERCEL_TOKEN` | Vercel 계정 토큰 |
| `VERCEL_ORG_ID` | Vercel 팀/계정 ID |
| `VERCEL_PROJECT_ID` | Vercel 프로젝트 ID |

현재 로컬 Vercel 프로젝트 정보 기준:

```txt
VERCEL_ORG_ID=team_HcpBhmwLzlFW6AFpQLhw5cda
VERCEL_PROJECT_ID=prj_TdIXv5pKDO3gsaaUBVTEJl4IzoyH
```

`VERCEL_TOKEN`은 Vercel 대시보드의 Account Settings > Tokens에서 발급한다.

## 주의사항

- Vercel의 GitHub 기본 연동도 켜져 있으면 push 1회에 배포가 두 번 실행될 수 있다.
- 중복 배포가 발생하면 Vercel 대시보드에서 Git 연동 자동 배포를 끄거나, 이 GitHub Actions 워크플로우를 비활성화한다.
- `.vercel/` 폴더는 로컬 설정이므로 git에 포함하지 않는다.
